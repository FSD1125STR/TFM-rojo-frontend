import { DividerProps } from './Divider.props';

export function Divider({ className = '' }) {
  return <div test-id="el-y5z6a7b8" className={`divider ${className}`}></div>;
}

Divider.propTypes = DividerProps;
